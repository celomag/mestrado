function getCriterios(){
    var cboxes = document.getElementsByName('checkCrit');
    var len = cboxes.length;
    
    return cboxes;
    // for (var i=0; i<len; i++) {
    //     cboxes[i].checked
    //     //alert(i + (cboxes[i].checked?' checked ':' unchecked ') + cboxes[i].value);
    // }
}